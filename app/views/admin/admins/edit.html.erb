<div class="row">
  <div class="large-6 columns">
    <%= form_for [:admin, @admin] do |f| %>
      <%= f.email_field :email %>
      <%= f.text_field :name %>
      <%= f.submit "Submit", :disable_with => 'Submiting...', class: "button" %>
    <% end %>
  </div>
</div>
<%= link_to "Change Password", admin_edit_password_path(current_admin) %>

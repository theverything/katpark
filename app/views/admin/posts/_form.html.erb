<%= form_for [:admin, @post] do |f| %>
  <%= f.label :title, "Title" %><br>
  <%= f.text_field :title %>
  <%= f.label :body, "Body" %><br>
  <%= f.text_area :body %>
  <%= f.submit :disable_with => 'Create Posting...' %>
<% end %>
